PHOTOX


1 - Prerequisites

 1.1 dependencies:
	 python 2.7.x - gtk 3.x - PILLOW (replaced PIL) - cv2
	 sudo yum install python
	 sudo yum install python-dev*
	 sudo yum install python-pip
	 sudo yum install numpy opencv*
	 pip install PILLOW
	 pip uninstall PIL

2 - Install
    run command 'python photox.py'

3 - HowTo

4 - Changelog
    2014-08-21
    1) photox now works with PILLOW

5 - Known Issues
    1) after using crop, tag people function the undo doesn't work
    2) resize function to be implemented
    3) edit and help menu to be implemented
    4) no image icons
